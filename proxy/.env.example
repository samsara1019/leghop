# 로컬(`vercel dev`)에서는 이 파일을 .env.local 로 복사해 쓴다.
# 배포 환경에는 Vercel 대시보드 또는 `vercel env add` 로 등록한다.

GEMINI_API_KEY=

# 선택 — 기본 gemini-3.6-flash
GEMINI_MODEL=

# 쉼표 구분. 비우면 localhost:5173만 허용.
# 웹앱 배포 후 실제 도메인을 반드시 추가할 것 (없으면 403 origin_not_allowed)
ALLOWED_ORIGINS=
