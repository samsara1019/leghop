# Leghop — 여행 계획 → 구간별 경로 자동 생성 웹앱

> 설계 문서 v0.3 · 2026-08-03
> 상태: **검토 대기** (승인 후 구현 착수)
> v0.2 변경: 오프라인 지도 제거 · 텍스트 파서를 Gemini API로 확정
> v0.3 변경: 프로젝트명 `TripRoute` → **`Leghop`** 확정 (leghop.com 확보 예정)

---

## 1. 제품 정의

### 한 줄 정의
여행 일정을 장소 단위로 입력해두면, 하루의 동선을 **구간(leg)별 이동 경로**로 자동 계산해서 앱 안에서 보여주는 웹앱. 일정과 경로 안내는 네트워크 없이도 열린다.

### 해결하는 문제
지금은 이렇게 한다:
1. 노션/메모장에 "공항 → 숙소 → 라 플라우타" 라고 적어둔다
2. 현지에서 매번 구글맵을 열고, 출발지·목적지를 손으로 타이핑한다

Leghop은 이 둘을 없앤다. 장소는 **미리** 등록해두고, 순서만 배치하면 구간 경로가 **자동으로** 붙는다.

이름의 `leg`는 설계 전반에서 쓰는 핵심 개념 — 일정의 한 칸에서 다음 칸으로 넘어가는 **이동 구간** — 을 그대로 가리킨다 (§4).

### 핵심 사용 시나리오 (사용자 원문 예시 기반)

```
[Day 1 · 바르셀로나]
 ─ 09:40  BCN 공항 도착
 ─ 09:40~11:00  입국 심사 + 짐 찾기          ← 이동 없는 활동 (80분 점유)
 ↓ 이동   Aerobus 35분 €7.25  /  택시 25분 €39   ← 대안 비교
 ─ 11:35  숙소 체크인 (Hotel Casa Bonay)
 ↓ 이동   도보 12분 · 950m
 ─ 13:00  라 플라우타 — 꿀대구
```

- `입국 심사 + 짐 찾기` 처럼 **장소가 아닌 활동**도 일정에 들어간다 → 시간만 점유, 경로 계산 없음
- `공항버스 또는 택시` 처럼 **수단이 미정**인 구간 → 대안을 나란히 비교
- 각 `↓ 이동`은 탭하면 앱 안에서 지도 + turn-by-turn

---

## 2. 확정된 설계 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 형태 | **모바일 우선 웹앱 (PWA)** | 앱스토어 심사 없이 배포, 홈 화면 설치 가능 |
| 지도 이탈 | **없음** — 앱 안에서 지도 렌더링 | Google Maps JS API 임베드 |
| Google 연동 | **Places + Directions 풀 연동** | 장소 검색 품질(현지 맛집 한글/원어 혼용 검색)이 오픈 데이터 대비 압도적 |
| 오프라인 | **데이터만** — 일정·장소·경로 텍스트. **지도는 온라인 전용** | §5 |
| 데이터 | **로컬 우선 (IndexedDB)**, 계정/동기화는 v2 | 초기 복잡도 최소화 |
| 텍스트 파서 | **Gemini API** (`gemini-3.6-flash`) | §8 |
| 백엔드 | **Cloudflare Worker 1개** (Gemini 중계 전용) | API 키 은닉 |

---

## 3. 화면 구성

### 3.1 화면 목록

| # | 화면 | 역할 |
|---|---|---|
| S1 | 여행 목록 | 여행 생성 / 진행중 여행 바로 열기 |
| S2 | **장소 서랍** | 숙소·맛집·관광지를 미리 등록해두는 풀 |
| S3 | **일자별 플래너** | 서랍에서 장소를 끌어와 하루 동선 구성 |
| S4 | 구간 상세 | 지도 + turn-by-turn + 대안 수단 비교 |
| S5 | **오늘 (여행 중 모드)** | 오늘 일정만, "다음 이동" 카드 최상단 |
| S6 | 텍스트 붙여넣기 | 메모를 통째로 붙여넣어 일정으로 변환 |

### 3.2 S2 — 장소 서랍

사용자 요구의 "숙소, 어디갈지(맛집, 관광지)정도는 미리 입력했다가 선택" 에 해당하는 화면.

```
┌─────────────────────────────────┐
│ 🔍 장소 검색                     │  ← Places Autocomplete
├─────────────────────────────────┤
│ [전체] [🏨숙소] [🍽맛집] [📷관광] │  ← 카테고리 필터
├─────────────────────────────────┤
│ 🏨 Hotel Casa Bonay              │
│    Gran Via 700 · 체크인 15:00   │
├─────────────────────────────────┤
│ 🍽 La Flauta                     │
│    꿀대구 (bacalao con miel)     │  ← 사용자 메모
│    12:00–16:00, 19:30–23:30      │  ← Places 영업시간
├─────────────────────────────────┤
│ 📷 Sagrada Família               │
│    ⚠️ 사전 예약 필요             │
└─────────────────────────────────┘
        [ + 지도에서 핀 찍기 ]
```

등록 경로 3가지:
1. 검색 (Places Autocomplete) — 주 경로
2. 지도에서 롱프레스로 핀 찍기 — 구글맵에 없는 곳
3. 텍스트 붙여넣기 일괄 등록 (S6)

### 3.3 S3 — 일자별 플래너

```
┌──────────┬──────────────────────────┐
│ 장소 서랍 │  Day 1 · 8/12 (화)        │
│          │ ┌──────────────────────┐ │
│ 🏨 Casa  │ │ 09:40 BCN 공항 T1     │ │
│    Bonay │ └──────────────────────┘ │
│          │   ↓ 🚌 35분 · €7.25  [⇄] │ ← 탭: 수단 변경/비교
│ 🍽 La    │ ┌──────────────────────┐ │
│  Flauta  │ │ 11:35 Hotel Casa Bonay│ │
│          │ │       체크인 · 30분    │ │
│ 📷 Sagr- │ └──────────────────────┘ │
│   ada    │   ↓ 🚶 12분 · 950m       │
│          │ ┌──────────────────────┐ │
│ [드래그] │ │ 13:00 La Flauta       │ │
│          │ │       점심 · 90분      │ │
│          │ └──────────────────────┘ │
│          │   ⏱ 총 이동 47분          │
└──────────┴──────────────────────────┘
```

동작:
- 서랍 → 타임라인 드래그앤드롭
- 순서를 바꾸면 **영향받는 구간만** 재계산 (전체 재계산 아님 → API 호출 절약)
- 카드 사이 `↓` 영역이 Leg. 기본 수단은 거리 기반 자동 추정(도보 ≤1.2km, 그 외 대중교통)
- 이동 없는 활동(`입국 심사`)은 `↓` 없는 회색 카드로 삽입

### 3.4 S5 — 오늘 (여행 중 모드)

여행 기간 중 앱을 열면 기본 진입하는 화면. 정보 밀도를 낮추고 "지금 뭐 해야 하나" 하나만 답한다.

```
┌─────────────────────────────────┐
│ 📴 오프라인 — 저장된 일정 표시 중  │  ← 오프라인일 때만
├─────────────────────────────────┤
│  다음 이동                       │
│  Hotel Casa Bonay → La Flauta    │
│  🚶 도보 12분 · 950m             │
│  12:48 출발 권장                 │
│                                  │
│  1. Gran Via 서쪽으로 200m       │
│  2. Balmes 우회전 · 350m         │
│  3. Consell de Cent 좌회전 …     │
│                                  │
│  📍 La Flauta                    │
│     Balmes 164 · 12:00–16:00     │
│     [ 지도 보기 ]  ← 온라인일 때만 │
├─────────────────────────────────┤
│  오늘 남은 일정 ▾                │
└─────────────────────────────────┘
```

오프라인에서는 `지도 보기` 버튼이 비활성화되고, 대신 주소·좌표·turn-by-turn 텍스트가 그 역할을 대신한다.

---

## 4. 데이터 모델

IndexedDB (Dexie). 모든 엔티티는 로컬이 정본.

```ts
Trip {
  id, title, city,
  lat, lng,                      // 지도 초기 중심 + Places 검색 지역 편향 (P1에서 추가)
  countryCode?, timezone?, currency?,   // 채울 경로가 아직 없어 optional로 둠
  startDate, endDate,
  createdAt, updatedAt
}

Place {                          // 장소 서랍의 항목
  id, tripId,
  googlePlaceId?,                // 무기한 보관 가능 (§7)
  name, nameLocal?,              // "라 플라우타" / "La Flauta"
  category: 'lodging'|'food'|'sight'|'shop'|'transport'|'custom',
  lat, lng, address,
  note?,                         // "꿀대구", "사전 예약 필요"
  openingHours?, priceLevel?,    // Places 스냅샷 — 30일 TTL
  tags: string[],
  snapshotAt                     // TTL 판정 기준
}

Day { id, tripId, date, order }

Item {                           // 타임라인의 한 칸
  id, dayId, order,
  kind: 'stop' | 'activity',
  placeId?,                      // kind='stop' 일 때
  title?,                        // kind='activity' 일 때 ("입국 심사 및 짐 찾기")
  startAt?, durationMin,
  note?
}

Leg {                            // Item N → Item N+1 이동
  id, dayId, fromItemId, toItemId,
  selectedMode: 'transit'|'walking'|'driving'|'bicycling',
  alternatives: LegOption[],
  computedAt, staleReason?
}

LegOption {
  mode, label,                   // "Aerobus A1", "택시"
  distanceM, durationS, fareText?,
  polyline,                      // 온라인 지도 렌더링용
  steps: { text, distanceM, durationS, maneuver? }[]   // 오프라인에서 쓰는 본체
}
```

### 재계산 트리거
Leg는 다음 경우에 `stale` 표시 후 재계산:
- 인접 Item의 place가 바뀜
- Item 순서 변경
- 출발 예정 시각이 30분 이상 이동 (대중교통 시간표 영향)
- `computedAt`이 7일 이상 경과

---

## 5. 오프라인 범위

**지도는 오프라인 대상이 아니다.** Google Maps는 약관상 타일 오프라인 캐시가 금지되어 있고, 그 제약을 우회하려면 설계가 크게 복잡해진다. 지도를 포기하면 오프라인은 단순한 로컬 데이터 문제로 축소된다.

| | 온라인 | 오프라인 |
|---|---|---|
| 일정 (Trip/Day/Item) | ✅ | ✅ 로컬이 정본 |
| 장소 정보 (이름·주소·좌표·영업시간·메모) | ✅ | ✅ 스냅샷 |
| 경로 요약 (수단·소요시간·거리·요금) | ✅ | ✅ 캐시 |
| turn-by-turn 텍스트 | ✅ | ✅ 캐시 |
| **지도 렌더링** | ✅ 인터랙티브 | ❌ 버튼 비활성 |
| 장소 검색 | Places Autocomplete | 등록된 장소 내 로컬 검색만 |
| 경로 재계산 | ✅ | ❌ 온라인 복귀 시 자동 |

### 구현
- 사용자 데이터(Trip/Place 메모/Item 순서)는 **항상** IndexedDB에 있다 — 별도 다운로드 불필요
- Directions 결과는 계산 시점에 자동 캐시. 별도 "팩 빌드" 단계 없음
- Service Worker는 앱 셸(JS/CSS/폰트/아이콘)만 precache
- Google 타일 요청은 SW에서 **명시적 캐시 제외** (약관 준수)
- iOS Safari가 미사용 기간 후 IndexedDB를 지울 수 있으므로 `navigator.storage.persist()` 요청 + 홈 화면 설치 유도
- Directions/Places 캐시는 30일 TTL (§7). 만료 시 캐시만 비우고 사용자 데이터는 보존

---

## 6. 기술 스택

### 프론트엔드 (P0에서 실제 설치된 버전)
```
Node 24 · React 19.2 · TypeScript 6.0 · Vite 8.2
├─ 라우팅      react-router 8.3        (v7 예상 → 실제 v8)
├─ 상태        zustand 5.0
├─ 영속        dexie 4.4 + dexie-react-hooks
├─ 지도        @vis.gl/react-google-maps 1.9
├─ DnD         @dnd-kit/core 6.3 + sortable 10
├─ PWA         vite-plugin-pwa 1.3 (Workbox)
├─ 스타일      tailwindcss 4.3 (@tailwindcss/vite, CSS-first 설정)
└─ 린트        oxlint 1.75             (create-vite 기본값이 eslint에서 교체됨)
```

> Node 18은 Vite 8·Wrangler 4가 거부한다. 저장소 루트 `.nvmrc`로 24를 고정했다.
> 사용자 전역 기본값은 18이라 `nvm use`가 필요하다.

### 백엔드 — Cloudflare Worker 1개

| 엔드포인트 | 용도 |
|---|---|
| `POST /parse` | 텍스트 → 일정 구조화 (Gemini API 중계, 키 은닉) |

Places / Directions / Maps JS는 **브라우저에서 JS API 라이브러리로 직접 호출**한다. 이 경우 API 키는 HTTP referrer 제한으로 보호되므로 프록시가 필요 없다.
(Web Service REST 엔드포인트를 직접 부르면 referrer 제한이 안 되므로 반드시 JS API 라이브러리를 쓸 것)

### 키 관리
- **Google Maps 키** — 브라우저 노출. referrer 제한 필수 + 일 쿼터 상한 설정
- **Gemini 키** — Worker 환경변수. 브라우저에 절대 내려가지 않음. Worker에 IP 기준 rate limit 적용

---

## 7. 제약 및 리스크

### 7.1 Google Maps Platform 약관

1. **콘텐츠 캐시 30일 상한** (`place_id`는 예외적으로 무기한 보관 가능)
   → Places 스냅샷·Directions 캐시의 TTL 근거
2. **타일 오프라인 저장 금지**
   → §5에서 오프라인 지도를 범위에서 뺀 이유이자, SW 캐시 제외 규칙의 근거
3. **비-Google 지도 위에 Google 데이터 표시 금지**
   → Directions 폴리라인을 MapLibre/OSM 지도에 얹는 안은 불가

### 7.2 요금
> ⚠️ **구현 착수 전 현행 요금표 재확인 필요.** Google Maps Platform은 2025년에 과금 체계를 개편해 기존 월 $200 크레딧 대신 SKU별 월간 무료 호출량 방식으로 바뀌었다. 정확한 무료 한도와 SKU 구분은 콘솔에서 확인하고 이 문서를 갱신할 것.

비용이 몰리는 지점과 대응:
- **Places Autocomplete** — 가장 비쌈. 세션 토큰 필수, 입력 디바운스 400ms
  - 최소 글자수는 **2글자**로 구현했다(설계 초안은 3글자). 한국어는 "성당", "공항"처럼
    2글자로 의미가 서서 3글자로 막으면 검색이 안 되는 경우가 많다. 비용이 문제되면 여기부터 조인다.
- **Directions** — Leg 단위 캐시 + 영향받은 구간만 재계산
  - 자동 계산은 **주 수단 1개만** 부른다(직선거리로 도보/대중교통 추정).
    대안 수단 3개는 사용자가 구간 상세에서 "다른 수단 조회"를 눌렀을 때만 부르고 캐시한다.
    타임라인에 4수단을 전부 미리 채우면 구간당 호출이 4배가 된다.
  - 경로를 못 찾은 구간(ZERO_RESULTS)은 `computedAt`을 남겨 재시도 폭주를 막는다.
- **Gemini** — 텍스트 파싱은 붙여넣기 1회당 1콜. `gemini-3.6-flash` 기준 사실상 무시 가능한 수준

### 7.3 그 외 리스크

| 리스크 | 대응 |
|---|---|
| 대중교통 경로가 유럽 소도시에서 부정확 | 도보/택시 대안을 항상 함께 표시 |
| 현지에서 데이터가 안 터지면 지도를 못 봄 | turn-by-turn 텍스트 + 주소 + 좌표를 크게 보여주는 것으로 대체 (§3.4). 로밍/eSIM 사용을 전제로 하는 설계임을 명시 |
| iOS Safari의 IndexedDB 삭제 | `storage.persist()` + 홈 화면 설치 유도 |
| 텍스트 파서의 장소 매칭 오류 | 자동 확정 금지. 항상 후보 3개 제시 후 사용자 확인 |

---

## 8. 텍스트 붙여넣기 파서 (S6)

사용자가 이미 써둔 메모를 그대로 붙여넣는 경로. 온보딩 마찰을 없애는 핵심 기능.

**입력**
```
바르셀로나 공항 도착 후 입국 심사 및 짐 찾기
공항버스(Aerobus) 또는 택시로 시내 이동
라 플라우타 에서 꿀대구 먹기
```

**입력 종류를 먼저 갈라낸다 (구현 시 추가)**

붙여넣기는 두 가지 성격이 섞여 들어온다. LLM을 항상 부르는 건 낭비다.

| 클립보드 내용 | 판별 근거 | 처리 |
|---|---|---|
| 엑셀·구글시트 표 | `text/html`에 `<table>`이 실려온다 | **LLM 없이** 열 매핑으로 직접 변환. 비용·지연 0 |
| 탭 구분 텍스트 | 줄의 과반에 탭이 있다 | 위와 동일 |
| 산문 메모 | 그 외 | Gemini 파싱, 실패 시 규칙 기반으로 대체 |

쉼표(CSV)는 표로 취급하지 않는다 — 주소에 쉼표가 흔해서 열이 어긋난다.
Worker가 설정되지 않았어도 규칙 기반 파서로 동작한다. Gemini보다 부정확하지만
기능이 죽는 것보다는 낫고, 결과는 어차피 사용자가 확인한다.

**처리 파이프라인**

1. Worker `POST /parse` → Gemini API 호출
   - 모델: **`gemini-3.6-flash`** (토큰 효율·저비용. 품질 부족 시 `gemini-3.5-flash`로 승급)
   - `responseMimeType: "application/json"` + `responseSchema`로 **구조화 출력 강제** → 파싱 실패 없음
   - `thinkingConfig`는 최소로 (단순 추출 작업이라 추론 예산 불필요)

   응답 스키마:
   ```json
   {
     "type": "array",
     "items": {
       "type": "object",
       "properties": {
         "kind":       {"type":"string","enum":["stop","activity","transfer"]},
         "query":      {"type":"string"},   // stop: 장소 검색어
         "title":      {"type":"string"},   // activity: 활동명
         "modeHints":  {"type":"array","items":{"type":"string"}},
         "durationMin":{"type":"integer"},
         "note":       {"type":"string"}
       },
       "required": ["kind"]
     }
   }
   ```

   실제 출력:
   ```json
   [
     {"kind":"activity","title":"입국 심사 및 짐 찾기","durationMin":80,"note":"BCN 공항"},
     {"kind":"transfer","modeHints":["transit","driving"],"note":"Aerobus 또는 택시"},
     {"kind":"stop","query":"La Flauta Barcelona","note":"꿀대구"}
   ]
   ```

2. 각 `query`를 Places Text Search로 매칭 → **후보 3개 제시**
3. 사용자가 확인/수정 → Place + Item 일괄 생성

자동 확정하지 않는다. 동명 식당이 흔하고, 잘못 찍힌 핀 하나가 현지에서 30분을 날린다.

> 모델 ID는 착수 시점에 [Gemini API 모델 문서](https://ai.google.dev/gemini-api/docs/models)에서 재확인할 것.

---

## 9. 구현 로드맵

| Phase | 내용 | 산출물 |
|---|---|---|
| ~~**P0**~~ ✅ | 스캐폴딩, Dexie 스키마, PWA 설정, Worker 구현 | 환경 점검 화면 + 지도. 키 발급·Worker 배포는 사용자 몫으로 남음 |
| ~~**P1**~~ ✅ | Trip CRUD, 장소 서랍, Places Autocomplete, 지도 핀 등록 | 장소를 모아둘 수 있음 |
| ~~**P2**~~ ✅ | 일자별 플래너, DnD, Leg 자동 계산, 대안 수단 비교 | **핵심 가치 완성 — 여기서 한 번 실사용 검증** |
| ~~**P3**~~ ✅ | 붙여넣기 가져오기 (엑셀 표 / Gemini / 규칙 기반) | 메모·시트 붙여넣기로 일괄 생성 |
| **P4** | PWA 설치, 오프라인 데이터 접근, Today 화면 | 여행에 들고 갈 수 있음 |
| **P5** | 공유 링크, 일정 내보내기(캘린더/PDF) | 배포 준비 |

오프라인 지도가 빠지면서 P3·P4 순서를 바꿨다. 파서가 앞으로 오면 P2 검증 때 일정을 손으로 안 넣어도 되므로 검증 자체가 빨라진다.

P2 종료 시점에 실제 여행 일정 하나를 통째로 넣어보고, 계속할지 판단하는 걸 권한다.

---

## 10. 승인 전 확인 필요한 사항

1. ~~**프로젝트 이름**~~ — ✅ `Leghop` 확정 (v0.3)
2. **계정/동기화 없이 로컬 전용으로 시작** — 기기를 바꾸면 데이터가 안 넘어간다. v1 범위에서 빼는 게 맞는지.
3. **첫 대상 여행** — P2 검증에 쓸 실제 일정이 있으면 그 도시 기준으로 데이터를 준비해두면 좋다 (예시가 바르셀로나였는데 실제 계획인지?).
